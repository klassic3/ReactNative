Welcom to Money Manager, the expense tracker application
Made using node.js and express.js for the backend and react native (Expo) for the front

To start the back end, create a .env file with the following
PORT=
MONGO_URI=
JWT_SECRET=
JWT_EXPIRES_IN=

Then run the commands
npm i 
npm start
